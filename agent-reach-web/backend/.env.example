# ──────────────────────────────────────────────────────────────────────
# Agent Reach Web — Environment Configuration
# Copy this file to .env and fill in values before running.
# ──────────────────────────────────────────────────────────────────────
GROQ_API_KEY=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
# Application
APP_TITLE=Agent Reach Web
APP_VERSION=1.0.0

# Security — change this to a strong random string in production
API_SECRET_KEY=change-me-to-a-random-string

# CORS — comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Agent Reach binary path (auto-detected if left empty)
AGENT_REACH_BIN=

# Command timeout in seconds (how long a single CLI call can run)
COMMAND_TIMEOUT=120
